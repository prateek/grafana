/**
 * Overlay UI script (minimal - most overlay logic is in contentScript.ts)
 * This file exists primarily for the build system and potential future enhancements.
 */

console.log('[PromQL AI] Overlay script loaded');

// This file is currently minimal because the overlay is created and managed
// entirely by the content script. If we need client-side overlay logic in the future
// (e.g., for a standalone overlay page rather than injected DOM), we can add it here.

export {};
