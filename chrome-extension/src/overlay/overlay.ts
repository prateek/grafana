/**
 * Overlay Script
 *
 * This script runs in the context of overlay.html (if used).
 * Currently, the overlay is created dynamically in the content script,
 * so this file is mostly a placeholder.
 */

console.log('[Grafana Prom AI] Overlay script loaded');

// This file can be extended in the future if we need a separate overlay page
// For now, all overlay logic is in overlayManager.ts
